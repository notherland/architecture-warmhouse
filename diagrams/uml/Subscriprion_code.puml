@startuml
title Warmhouse Subscription Management Code Diagram

top to bottom direction

!include <C4/C4_Container>

class Subscription {
  +String name
  +Float cost
  +String interval
}

class UserSubscription {
  +UUID userId
  +UUID subscriptionId
  +Float userCost
  +SubStatusEnum status
  +Date startDate
  +Date endDate
  +void activate()
  +void cancel()
}

UserSubscription "1" -- "0..*" Subscription : has

@enduml